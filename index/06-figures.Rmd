\clearpage

# FIGURES

## Area 5ABCD

```{r fig-catch-5abcd, fig.cap="Catch for Area 5ABCD"}
make.catches.plot(catch.5)
```

```{r fig-base-biomass-5abcd, fig.cap="Biomass for the Reference Model, Area 5ABCD"}
b.plot(base.model.5abcd)
# make.biomass.mcmc.plot(
#   base.model.5abcd,
#   base.model.5abcd.name,
#   ylim = c(0, 100000),
#   opacity = 50)
```

```{r fig-base-mcmc-trace-5abcd, fig.cap="Traceplots of posterior samples for the Area 5ABCD base reference model. $\\bar{R} = R_{Avg}$, $q_1$ = Hecate Strait Assemblage survey, $q_2$ = Queen Charlotte Sound Synoptic Survey, $q_3$ = Hecate Strait Synoptic Survey, $q_4$ = Commercial CPUE pre-1996, and $q_5$ = Commercial CPUE post-1995."}
make.traces.plot(base.model.5abcd)
```

```{r fig-base-mcmc-priors-5abcd, fig.cap="Prior probability distributions used in the Area 5ABCD base reference model. $\\bar{R} = R_{Avg}$, $q_1$ = Hecate Strait Assemblage survey, $q_2$ = Queen Charlotte Sound Synoptic Survey, $q_3$ = Hecate Strait Synoptic Survey, $q_4$ = Commercial CPUE pre-1996, and $q_5$ = Commercial CPUE post-1995."}
make.priors.posts.plot(base.model.5abcd)
```

```{r fig-base-mcmc-autocor-5abcd, fig.cap="Autocorrelation plots for the Area 5ABCD base reference model. $\\bar{R} = R_{Avg}$, $q_1$ = Hecate Strait Assemblage survey, $q_2$ = Queen Charlotte Sound Synoptic Survey, $q_3$ = Hecate Strait Synoptic Survey, $q_4$ = Commercial CPUE pre-1996, and $q_5$ = Commercial CPUE post-1995."}
make.autocor.plot(base.model.5abcd)
```

```{r fig-base-mcmc-pairs-5abcd, fig.cap="Pairs plots of posterior samples for the Area 5ABCD base reference model. $\\bar{R} = R_{Avg}$, $q_1$ = Hecate Strait Assemblage survey, $q_2$ = Queen Charlotte Sound Synoptic Survey, $q_3$ = Hecate Strait Synoptic Survey, $q_4$ = Commercial CPUE pre-1996, and $q_5$ = Commercial CPUE post-1995."}
make.pairs.plot(base.model.5abcd)
```

\clearpage

## Area 3CD

```{r fig-catch-3cd, fig.cap="Catch for Area 3CD"}
make.catches.plot(catch.3)
```

```{r fig-base-biomass-3cd, fig.cap="Biomass for the Reference Model, Area 3CD"}
b.plot(base.model.3cd)
# make.biomass.mcmc.plot(
#   base.model.3cd,
#   base.model.3cd.name,
#   ylim = c(0, 100000),
#   opacity = 50)
```

```{r fig-base-mcmc-trace-3cd, fig.cap="Traceplots of posterior samples for the Area 3CD base reference model. $\\bar{R} = R_{Avg}$, $q_1$ = West Coast Vancouver Island Synoptic Survey, $q_2$ = Commercial CPUE pre-1996, $q_3$ = Commercial CPUE post-1995, and $q_3$ = NMFS Triennial Survey (Canadian portion)."}
make.traces.plot(base.model.3cd)
```

```{r fig-base-mcmc-priors-3cd, fig.cap="Prior probability distributions used in the Area 3CD base reference model. $\\bar{R} = R_{Avg}$, $q_1$ = West Coast Vancouver Island Synoptic Survey, $q_2$ = Commercial CPUE pre-1996, $q_3$ = Commercial CPUE post-1995, and $q_3$ = NMFS Triennial Survey (Canadian portion)."}
make.priors.posts.plot(base.model.3cd)
```

```{r fig-base-mcmc-autocor-3cd, fig.cap="Autocorrelation plots for the Area 3CD base reference model. $\\bar{R} = R_{Avg}$, $q_1$ = West Coast Vancouver Island Synoptic Survey, $q_2$ = Commercial CPUE pre-1996, $q_3$ = Commercial CPUE post-1995, and $q_3$ = NMFS Triennial Survey (Canadian portion)."}
make.autocor.plot(base.model.3cd)
```

```{r fig-base-mcmc-pairs-3cd, fig.cap="Pairs plots of posterior samples for the Area 3CD base reference model. $\\bar{R} = R_{Avg}$, $q_1$ = West Coast Vancouver Island Synoptic Survey, $q_2$ = Commercial CPUE pre-1996, $q_3$ = Commercial CPUE post-1995, and $q_3$ = NMFS Triennial Survey (Canadian portion)."}
make.pairs.plot(base.model.3cd)
```

